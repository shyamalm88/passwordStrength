import { PwdStrengthDirective } from './pwd-strength.directive';

describe('PwdStrengthDirective', () => {
  it('should create an instance', () => {
    const directive = new PwdStrengthDirective();
    expect(directive).toBeTruthy();
  });
});
